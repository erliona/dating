-r requirements.txt
pytest>=8.2,<9.0
pytest-asyncio>=0.23
pytest-cov>=4.1,<6.0
pytest-timeout>=2.1  # Required for CI test timeouts
pyyaml>=6.0  # Required for monitoring config tests
aiosqlite>=0.19
pip-audit>=2.6  # Security vulnerability scanner for dependencies
