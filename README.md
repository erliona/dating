# Dating Bot & WebApp

Telegram-бот для знакомств с интегрированным мини-приложением (WebApp). Проект построен на современном стеке: aiogram 3, SQLAlchemy 2.0 с асинхронным драйвером, PostgreSQL, Docker, и полностью автоматизированным CI/CD на GitHub Actions.

## 📋 Содержание

- [Архитектура и Компоненты](#архитектура-и-компоненты)
- [Бизнес-логика](#бизнес-логика)
- [Требования](#требования)
- [Переменные Окружения](#переменные-окружения)
- [Быстрый Старт](#быстрый-старт)
- [Развертывание через CI/CD](#развертывание-через-cicd)
- [Тестирование](#тестирование)
- [Структура Проекта](#структура-проекта)
- [Мониторинг и Отладка](#мониторинг-и-отладка)

---

## 🏗️ Архитектура и Компоненты

### Общая Архитектура

Система состоит из трех основных компонентов, работающих в Docker-контейнерах:

```
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   Telegram   │◄────►│   Bot App    │◄────►│  PostgreSQL  │
│    Users     │      │  (Python)    │      │   Database   │
└──────────────┘      └──────────────┘      └──────────────┘
       │                                              ▲
       │                                              │
       ▼                                              │
┌──────────────┐                                     │
│   WebApp     │                                     │
│   (nginx)    │─────────────────────────────────────┘
└──────────────┘
```

### Компоненты Системы

#### 1. **Telegram Bot (bot/)**

**Назначение**: Основная точка взаимодействия с пользователями через Telegram.

**Технологии**:
- **aiogram 3.x** - современный асинхронный фреймворк для Telegram Bot API
- **Python 3.11+** - основной язык разработки
- **SQLAlchemy 2.0** - ORM с асинхронным драйвером для работы с БД

**Ключевые модули**:
- `bot/main.py` - точка входа, обработчики команд и сообщений, бизнес-логика матчинга
- `bot/config.py` - загрузка и валидация конфигурации из переменных окружения
- `bot/db.py` - модели данных (ProfileModel) и репозиторий (ProfileRepository)

**Функциональность**:
- Обработка команд `/start`, `/cancel`, `/reset`
- Прием данных из мини-приложения через WebAppData
- Сохранение и обновление профилей в БД
- Алгоритм поиска взаимных совпадений (mutual matching)
- Отправка уведомлений о найденных парах

#### 2. **PostgreSQL Database (db)**

**Назначение**: Хранение пользовательских профилей и данных.

**Конфигурация**:
- PostgreSQL 15 Alpine (легковесный образ)
- Volume `postgres_data` для персистентности данных
- Health check для обеспечения готовности перед запуском бота

**Структура данных**:
- Таблица `profiles` с полями:
  - `id` (primary key, autoincrement)
  - `user_id` (unique, indexed) - Telegram ID пользователя
  - `name`, `age`, `gender`, `preference` - базовые данные
  - `bio`, `location` - опциональная информация
  - `interests` (JSONB) - список интересов
  - `goal` - цель знакомства
  - `photo_file_id`, `photo_url` - фотография профиля

#### 3. **WebApp (webapp/)**

**Назначение**: Telegram Mini App для заполнения анкеты пользователя.

**Технологии**:
- Статичный HTML/CSS/JavaScript
- Telegram WebApp API для интеграции
- nginx как веб-сервер

**Функциональность**:
- Форма создания/редактирования профиля
- Клиентская валидация данных
- Автосохранение черновика в localStorage
- Индикатор прогресса заполнения
- Отправка данных боту через Telegram WebApp API

#### 4. **Миграции (migrations/)**

**Назначение**: Управление схемой базы данных.

**Инструменты**:
- **Alembic** - инструмент миграций для SQLAlchemy
- Автоматическое применение миграций при старте бота (контролируется через `RUN_DB_MIGRATIONS`)

**Файлы**:
- `migrations/env.py` - конфигурация Alembic
- `migrations/versions/` - файлы миграций
- `alembic.ini` - настройки Alembic

#### 5. **CI/CD Pipeline (.github/workflows/)**

**Файлы**:
- `ci.yml` - тестирование и сборка при каждом push/PR
- `deploy.yml` - автоматическое развертывание на продакшн

**Подробнее**: см. раздел [Развертывание через CI/CD](#развертывание-через-cicd)

---

## 🎯 Бизнес-логика

### Жизненный Цикл Пользователя

```
1. Пользователь → /start → Показ кнопки "Открыть мини-приложение"
                              ↓
2. Открывает WebApp → Заполняет анкету → Отправка данных боту
                              ↓
3. Бот → Валидация данных → Сохранение в БД
                              ↓
4. Бот → Поиск mutual match → Если найдено → Отправка анкет обоим
                              ↓
5. Пользователи → Продолжают общение в Telegram
```

### Алгоритм Поиска Совпадений (Mutual Matching)

При сохранении **нового** профиля бот выполняет поиск совпадений:

1. **Фильтр по гендеру**: 
   - Если пользователь ищет конкретный гендер (male/female/other), система фильтрует по этому критерию
   - Если пользователь выбрал "any", фильтр не применяется

2. **Взаимная совместимость**:
   - Система проверяет, что найденный профиль также заинтересован в гендере текущего пользователя
   - Учитывается "any" preference как универсальная совместимость

3. **Исключение себя**:
   - Профиль не может совпасть сам с собой

4. **Первое совпадение**:
   - Возвращается первый подходящий профиль (LIMIT 1)
   - Это базовая версия, в будущем можно добавить более сложное ранжирование

### Валидация Данных

**На уровне бота (Python)**:
- Обязательные поля: `name`, `age`, `gender`, `preference`
- Минимальный возраст: 18 лет
- Формат photo_url: только HTTPS
- Нормализация гендеров/предпочтений (русский/английский → единый формат)
- Нормализация целей знакомства

**На уровне WebApp (JavaScript)**:
- Real-time валидация полей
- Проверка корректности ввода
- Предупреждения о пустых обязательных полях

### Обработка Фотографий

Система поддерживает два способа добавления фото:
1. **photo_url** - HTTPS ссылка на фото (валидируется)
2. **photo_file_id** - File ID из Telegram (для будущей функциональности прямой загрузки)

### Обновление и Удаление Профиля

- **Обновление**: повторная отправка данных из WebApp обновляет существующий профиль
- **Удаление**: отправка `{"action": "delete"}` из WebApp удаляет профиль из БД

---

## 📦 Требования

### Для локальной разработки:
- **Python 3.11+**
- **PostgreSQL 14+** (или используйте Docker Compose)
- **Docker & Docker Compose** (для контейнерного запуска)

### Для продакшн развертывания:
- **Сервер** с SSH доступом
- **Docker & Docker Compose** на сервере
- **Telegram Bot Token** (получить у @BotFather)
- **Публичный URL** для WebApp (опционально, но рекомендуется)

---

## 🔐 Переменные Окружения

Полное описание переменных окружения см. в файле [`.env.example`](.env.example).

### Обязательные переменные:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BOT_TOKEN` | Токен Telegram-бота от @BotFather | `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11` |

### Настройки базы данных:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `POSTGRES_DB` | Имя базы данных PostgreSQL | `dating` |
| `POSTGRES_USER` | Пользователь PostgreSQL | `dating` |
| `POSTGRES_PASSWORD` | Пароль PostgreSQL | `dating` |
| `POSTGRES_PORT` | Порт PostgreSQL | `5432` |
| `BOT_DATABASE_URL` | Прямая строка подключения (опционально) | автоматически из `POSTGRES_*` |

### Опциональные переменные:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `WEBAPP_URL` | URL мини-приложения | `http://localhost:8080` |
| `WEBAPP_PORT` | Порт для nginx | `8080` |
| `RUN_DB_MIGRATIONS` | Запускать ли миграции при старте | `true` |
| `DEBUG` | Включить DEBUG логирование | `false` |

---

## 🚀 Быстрый Старт

### Вариант 1: Docker Compose (Рекомендуется)

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/erliona/dating.git
   cd dating
   ```

2. **Создайте `.env` файл**:
   ```bash
   cp .env.example .env
   nano .env  # или любой другой редактор
   ```
   
   Обязательно укажите реальный `BOT_TOKEN`.

3. **Запустите все сервисы**:
   ```bash
   docker compose up -d --build
   ```

4. **Проверьте логи**:
   ```bash
   docker compose logs -f bot
   ```

5. **Остановка**:
   ```bash
   docker compose down
   ```

### Вариант 2: Локальный запуск (для разработки)

1. **Создайте виртуальное окружение**:
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # Windows: .venv\Scripts\activate
   ```

2. **Установите зависимости**:
   ```bash
   pip install --upgrade pip
   pip install -r requirements.txt
   pip install -r requirements-dev.txt  # для тестов
   ```

3. **Настройте переменные окружения**:
   ```bash
   export BOT_TOKEN="your-token-here"
   export BOT_DATABASE_URL="postgresql+asyncpg://user:pass@localhost:5432/dating"
   export WEBAPP_URL="http://localhost:8080"  # опционально
   ```

4. **Примените миграции**:
   ```bash
   alembic upgrade head
   ```

5. **Запустите бота**:
   ```bash
   python -m bot.main
   ```

6. **Для WebApp запустите простой сервер** (в отдельном терминале):
   ```bash
   python -m http.server 8080 --directory webapp
   ```

---

## 🔄 Развертывание через CI/CD

Проект использует GitHub Actions для автоматического развертывания. Весь деплой происходит **только** через CI/CD - ручная работа с сервером не требуется.

### Настройка Secrets в GitHub

Перейдите в **Settings → Secrets and variables → Actions** и добавьте:

| Secret | Описание | Пример |
|--------|----------|--------|
| `DEPLOY_HOST` | IP или hostname сервера | `123.45.67.89` или `server.example.com` |
| `DEPLOY_USER` | SSH пользователь с правами Docker | `deploy` |
| `DEPLOY_PATH` | Путь на сервере для проекта | `/opt/dating` |
| `DEPLOY_SSH_KEY` | Приватный SSH ключ (PEM или base64) | Содержимое `~/.ssh/id_ed25519` |
| `ENV_FILE` | Содержимое `.env` файла для продакшн | Многострочный секрет с переменными |
| `DEPLOY_PORT` | SSH порт (опционально) | `22` (по умолчанию) |

### Workflow: CI (Тестирование)

**Файл**: `.github/workflows/ci.yml`

**Триггеры**: 
- Push в ветки `main`/`master`
- Pull requests в `main`/`master`

**Этапы**:
1. Checkout кода
2. Установка Python 3.11
3. Установка зависимостей
4. Запуск тестов (`pytest`)
5. Сборка Docker образа (без push)

### Workflow: Deploy (Развертывание)

**Файл**: `.github/workflows/deploy.yml`

**Триггеры**:
- Push в ветку `main`
- Ручной запуск через `workflow_dispatch`

**Этапы**:

1. **Проверка секретов**:
   - Валидация наличия всех обязательных секретов
   - Пропуск деплоя, если секреты не настроены

2. **Настройка SSH**:
   - Загрузка приватного ключа
   - Поддержка PEM формата и base64-кодирования
   - Добавление ключа в ssh-agent

3. **Добавление сервера в known_hosts**:
   - SSH keyscan для автоматического принятия ключа сервера
   - Предотвращение интерактивных запросов

4. **Подготовка директории на сервере**:
   - Создание `DEPLOY_PATH` если не существует
   - Установка прав доступа

5. **Загрузка проекта**:
   - Rsync файлов проекта на сервер
   - Исключение `.git` и `.github`

6. **Развертывание**:
   - Запись `.env` файла из секрета
   - Выполнение `docker compose up -d --build`
   - Вывод статуса контейнеров

### Ручной деплой (альтернатива)

Если нужно развернуть вручную, используйте скрипт `scripts/deploy.sh`:

```bash
scripts/deploy.sh \
  -H server.example.com \
  -u deploy \
  -d /opt/dating \
  -e .env.production \
  -i ~/.ssh/id_ed25519
```

**Параметры**:
- `-H` - hostname или IP сервера
- `-u` - SSH пользователь
- `-d` - путь на сервере
- `-e` - локальный `.env` файл для загрузки
- `-i` - путь к SSH ключу (опционально)
- `-p` - SSH порт (опционально, по умолчанию 22)

---

## 🧪 Тестирование

### Запуск Тестов

```bash
# Все тесты
python -m pytest

# С подробным выводом
python -m pytest -v

# Только определенный файл
python -m pytest tests/test_config.py

# Один конкретный тест
python -m pytest tests/test_config.py::TestBotConfig::test_load_config_success
```

### Покрытие Тестов

Проект включает **99 тестов**, покрывающих:

#### 1. **Конфигурация** (`tests/test_config.py`) - 12 тестов
- Валидация обязательных переменных (`BOT_TOKEN`, `BOT_DATABASE_URL`)
- Проверка формата database URL
- Ограничение на только PostgreSQL
- Валидация host и database name
- Fallback на `DATABASE_URL`

#### 2. **База данных** (`tests/test_database.py`) - 13 тестов
- **ProfileModel**:
  - Создание модели
  - Конвертация в/из Profile dataclass
  - Обновление данных
- **ProfileRepository**:
  - Upsert (создание и обновление)
  - Получение профиля
  - Удаление профиля
  - Поиск mutual match с различными сценариями

#### 3. **Бизнес-логика** (`tests/test_bot_logic.py`) - 74 теста
- **Нормализация гендера/предпочтений**: 24 теста
  - Поддержка русского и английского
  - Различные варианты написания
  - Обработка невалидных значений
- **Нормализация целей знакомства**: 38 тестов
  - 4 основные категории (serious, friendship, networking, casual)
  - Множественные алиасы на русском и английском
  - Валидация ошибочных значений
- **Построение профиля из payload**: 12 тестов
  - Полный набор полей
  - Минимальный набор
  - Валидация обязательных полей
  - Проверка возраста 18+
  - Валидация HTTPS URL
  - Обработка интересов (list и string)
  - Корректная обработка пустых optional полей

### Структура Тестов

```
tests/
├── conftest.py           # Фикстуры (in-memory БД, репозиторий)
├── test_config.py        # Тесты конфигурации
├── test_database.py      # Тесты БД и моделей
└── test_bot_logic.py     # Тесты бизнес-логики
```

### CI Интеграция

Тесты автоматически запускаются в GitHub Actions при каждом:
- Push в `main`/`master`
- Создании Pull Request

Деплой **не произойдет**, если тесты не прошли успешно.

---

## 📁 Структура Проекта

```
.
├── .github/
│   └── workflows/
│       ├── ci.yml              # CI: тесты и сборка
│       └── deploy.yml          # CD: автоматический деплой
├── bot/
│   ├── __init__.py
│   ├── config.py               # Загрузка конфигурации
│   ├── db.py                   # Модели и репозиторий
│   └── main.py                 # Основная логика бота
├── docker/
│   └── entrypoint.sh           # Entrypoint для Docker контейнера
├── migrations/
│   ├── env.py                  # Конфигурация Alembic
│   └── versions/               # Файлы миграций
├── scripts/
│   └── deploy.sh               # Скрипт ручного деплоя
├── tests/
│   ├── conftest.py             # Pytest fixtures
│   ├── test_bot_logic.py       # Тесты бизнес-логики
│   ├── test_config.py          # Тесты конфигурации
│   └── test_database.py        # Тесты БД
├── webapp/
│   ├── css/                    # Стили мини-приложения
│   ├── js/                     # JavaScript логика
│   └── index.html              # Главная страница WebApp
├── .dockerignore
├── .env.example                # Шаблон переменных окружения
├── .gitignore
├── alembic.ini                 # Конфигурация Alembic
├── docker-compose.yml          # Описание сервисов
├── Dockerfile                  # Образ бота
├── pytest.ini                  # Конфигурация pytest
├── README.md                   # Этот файл
├── requirements.txt            # Основные зависимости
└── requirements-dev.txt        # Зависимости для разработки
```

---

## 📊 Мониторинг и Отладка

### Просмотр Логов

#### Docker Compose:

```bash
# Все сервисы
docker compose logs -f

# Только бот
docker compose logs -f bot

# Только БД
docker compose logs -f db

# Последние 100 строк
docker compose logs --tail=100 bot
```

#### Уровни Логирования

По умолчанию: **INFO**

Включить DEBUG:
```bash
# В .env
DEBUG=true
```

Или при запуске:
```bash
DEBUG=true docker compose up -d --build
```

### Проверка Состояния Контейнеров

```bash
# Статус всех сервисов
docker compose ps

# Подробная информация
docker compose ps -a

# Использование ресурсов
docker stats
```

### Подключение к Базе Данных

```bash
# Через psql в контейнере
docker compose exec db psql -U dating -d dating

# Примеры запросов:
# \dt - список таблиц
# SELECT * FROM profiles; - все профили
# SELECT COUNT(*) FROM profiles; - количество профилей
```

### Типичные Проблемы и Решения

#### 1. Бот не запускается

**Симптом**: Контейнер `bot` постоянно перезапускается

**Возможные причины**:
- Неверный `BOT_TOKEN`
- БД недоступна
- Ошибки в миграциях

**Решение**:
```bash
docker compose logs bot  # Проверить логи
docker compose ps db     # Проверить статус БД
```

#### 2. База данных не подключается

**Симптом**: `Cannot connect to database`

**Решение**:
```bash
# Проверить healthcheck БД
docker compose ps db

# Перезапустить БД
docker compose restart db

# Пересоздать с нуля
docker compose down -v  # ВНИМАНИЕ: удалит данные
docker compose up -d --build
```

#### 3. WebApp недоступен

**Симптом**: 404 при открытии мини-приложения

**Решение**:
- Проверить `WEBAPP_URL` в `.env`
- Убедиться, что nginx контейнер запущен: `docker compose ps webapp`
- Проверить порт: по умолчанию `8080`

#### 4. Миграции не применяются

**Симптом**: Ошибки при сохранении профилей

**Решение**:
```bash
# Применить миграции вручную
docker compose exec bot alembic upgrade head

# Или пересоздать контейнер
docker compose up -d --build bot
```

### Мониторинг в Production

Рекомендуется настроить:
- **Logging**: Централизованный сбор логов (ELK Stack, Grafana Loki)
- **Metrics**: Prometheus + Grafana для метрик контейнеров
- **Alerting**: Настроить алерты на критичные ошибки
- **Backups**: Регулярное резервное копирование БД

---

## 🔧 Работа с Миграциями

### Создание Новой Миграции

```bash
# Автоматическая генерация на основе изменений в моделях
alembic revision --autogenerate -m "Add new field to profile"

# Ручная миграция
alembic revision -m "Custom migration"
```

### Применение Миграций

```bash
# До последней версии
alembic upgrade head

# До конкретной ревизии
alembic upgrade <revision_id>

# На одну вперед
alembic upgrade +1
```

### Откат Миграций

```bash
# На одну назад
alembic downgrade -1

# До конкретной ревизии
alembic downgrade <revision_id>

# К самому началу (осторожно!)
alembic downgrade base
```

### История Миграций

```bash
# Текущая версия
alembic current

# История всех миграций
alembic history

# Подробная история
alembic history --verbose
```

---

## 📝 Разработка

### Добавление Новой Функциональности

1. **Создайте ветку**:
   ```bash
   git checkout -b feature/new-feature
   ```

2. **Внесите изменения** в код

3. **Добавьте тесты** в `tests/`

4. **Запустите тесты**:
   ```bash
   python -m pytest
   ```

5. **Создайте миграции** (если меняли модели):
   ```bash
   alembic revision --autogenerate -m "Description"
   ```

6. **Commit и Push**:
   ```bash
   git add .
   git commit -m "Add new feature"
   git push origin feature/new-feature
   ```

7. **Создайте Pull Request** на GitHub

8. **CI автоматически**:
   - Запустит тесты
   - Проверит сборку Docker
   - Заблокирует merge если есть ошибки

### Code Style

Проект использует:
- Type hints для всех функций
- Docstrings для модулей и классов
- Async/await для асинхронных операций
- SQLAlchemy 2.0 стиль (async ORM)

---

## 📄 Лицензия

Этот проект создан для образовательных целей.

---

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел [Мониторинг и Отладка](#мониторинг-и-отладка)
2. Просмотрите Issues на GitHub
3. Создайте новый Issue с описанием проблемы и логами

---

**Успешных знакомств! ❤️**
